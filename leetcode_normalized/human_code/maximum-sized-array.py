import bisect
v1, v2 = (1, 0)
v3 = [0]

class C1(object):

    def maxSizedArray(self, a1):
        """
        """
        global i, area
        while v3[-1] <= a1:
            v1 += 2 * ((v1 - 1) * v1 + sum(((v1 - 1 >> bit + 1) * (1 << bit) * (1 << bit) for v2 in range((v1 - 1).bit_length()) if not v1 - 1 & 1 << v2))) - (v1 - 1 | v1 - 1)
            v3.append((0 + (v1 - 1)) * v1 // 2 * v1)
            v3 += 1
        return bisect.bisect_right(v3, a1) - 1
